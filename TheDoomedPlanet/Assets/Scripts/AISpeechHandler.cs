using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AISpeechHandler : MonoBehaviour
{
    // Config Parameters
    [SerializeField] string[] dialogueInputs = null;

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hi");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
